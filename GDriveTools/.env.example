# Google OAuth app credentials -- from Google Cloud Console
# Reuses the same GCP OAuth client already set up for GmailTools (no new app
# registration needed); this file just holds GDriveTools' own copy of it and
# its own token, kept independent of GmailTools' .env/tokens.
GDRIVE_CLIENT_ID=
GDRIVE_CLIENT_SECRET=
